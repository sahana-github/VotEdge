import os
from datetime import timedelta
from dotenv import load_dotenv

# Load environment variables
load_dotenv()

# Airflow configuration
AIRFLOW_HOME = os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))
os.environ['AIRFLOW_HOME'] = AIRFLOW_HOME

# Database configuration
SQL_ALCHEMY_CONN = 'sqlite:///' + os.path.join(AIRFLOW_HOME, 'airflow.db')

# Core configuration
CORE__EXECUTOR = 'SequentialExecutor'
CORE__LOAD_EXAMPLES = False
CORE__DAGS_ARE_PAUSED_AT_CREATION = False

# Logging configuration
LOGGING__LOGGING_LEVEL = 'INFO'
LOGGING__FAB_LOGGING_LEVEL = 'WARN'

# API configuration
API__AUTH_BACKEND = 'airflow.api.auth.backend.basic_auth'